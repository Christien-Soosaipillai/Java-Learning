package com.christien.javaeight.unit4;

public interface Operation {
    Integer someNum = 4;                    //Static and final by default because interfaces can't be instantiated

    Integer calculate(Integer a, Integer b);

}
